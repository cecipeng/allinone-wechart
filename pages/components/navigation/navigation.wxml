<view class='ui-navigation'>
  <view wx:for="{{navList}}" wx:key="{{item.value}}" data-id='{{item.value}}' class="ui-navigation__item {{item.classname}}" bindtap='switchTab'>{{item.name}}</view>
</view>